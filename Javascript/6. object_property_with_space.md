# object property with space or dash:
- use `"foor-far"` parentisis to wrap the string
- get the value with bracket: `obj["foo-far"]`
-  


```
var obj = {
   "foo-bar": "Foo Bar"
};

obj["foo-bar"]\\ returns "Foo Bar"

```
